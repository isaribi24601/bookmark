<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>Document</title>
  <link rel="stylesheet" href="/assets/css/bootstrap.min.css">
  <script src="//code.jquery.com/jquery-1.11.2.min.js"></script>
</head>
<body>
  <form id="frm" action="/create" method="post" class="pull-left">
    <label for="url">URL</label>
    <input type="url" id="url" name="url">
    <label for="title">Title</label>
    <input type="title" id="title" name="title">
    <input type="submit" value="追加" name="commit" class="btn btn-primary">
    </form>
    
    <script>
        $('#url').change(function() {
     $.get("/api/site", { url: $(this).val() },
           function(data){
         $('#title').val(data.title);
     });
        })
    </script>
    
    <div class="container-fluid">
    <div class="row">
        <% @bookmarks.each do |bookmark| %>
        <div class="col-md-3">
                    <img src="http://mozshot.nemui.org/shot/large?<%= bookmark.url %>" alt="" />
        <p><a href="<%= bookmark.url %>"><%= bookmark.title %></a></p>
        </div>
        <% end %>
        </div>
</body>
</html>
